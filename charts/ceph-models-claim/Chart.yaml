apiVersion: v2
name: ceph-models-claim
description: CephFS Secret + static PV + PVC for the `client.k0rdent-models` credential, pinned at /models on the `ai-shared` filesystem. Expects the ceph-csi-cephfs driver to already be installed (separate ServiceTemplate).
type: application
version: 0.1.0
appVersion: "1.0.0"
keywords:
  - ceph
  - cephfs
  - models
